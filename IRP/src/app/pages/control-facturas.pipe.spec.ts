import { ControlFacturasPipe } from './control-facturas.pipe';

describe('ControlFacturasPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlFacturasPipe();
    expect(pipe).toBeTruthy();
  });
});
