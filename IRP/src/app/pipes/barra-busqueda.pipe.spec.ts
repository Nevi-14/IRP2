import { BarraBusquedaPipe } from './barra-busqueda.pipe';

describe('BarraBusquedaPipe', () => {
  it('create an instance', () => {
    const pipe = new BarraBusquedaPipe();
    expect(pipe).toBeTruthy();
  });
});
